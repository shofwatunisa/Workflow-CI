name: text_emotion_env
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - mlflow
      - scikit-learn
      - pandas
      - matplotlib
      - seaborn
      - joblib
